services:
  spring-app:
    image: badzim/codelands:dpr-api-latest
    build:
      context: . # Contexte du projet
      dockerfile: docker/Dockerfile # Chemin vers le Dockerfile
    ports:
      - "8001:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: always
